[project]
name = "giters"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "eiogram>=2.0.1",
    "gitapi>=1.0.1",
    "python-decouple>=3.8",
]
